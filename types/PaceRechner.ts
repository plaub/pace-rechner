export enum PaceType {
    Pace = "Pace",
    Speed = "Speed"
}

export enum PaceUnit  {
    Run = "min/km",
    Swim = "min/100m",
}

export enum SpeedUnit  {
    Bike = "km/h",
}

export enum DistanceUnit  {
    Bike = "km",
    Run = "m",
    Swim = "m",
}